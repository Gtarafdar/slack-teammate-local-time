<?xml version="1.0" encoding="UTF-8"?>
<!--
  LaunchAgent for the Slack inline teammate-time tool.

  This is a TEMPLATE. The placeholder __PROJECT_DIR__ is replaced with the
  real project path by install-agent.sh, which writes the final file to
  ~/Library/LaunchAgents/com.user.slacktime.plist.

  It is a per-user agent (lives under ~/Library/LaunchAgents), so it needs
  NO admin rights. It starts at login, keeps the injector alive, and writes
  logs to the project directory.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.user.slacktime</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__PROJECT_DIR__/run.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__PROJECT_DIR__</string>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>__PROJECT_DIR__/agent.out.log</string>

    <key>StandardErrorPath</key>
    <string>__PROJECT_DIR__/agent.err.log</string>
</dict>
</plist>
